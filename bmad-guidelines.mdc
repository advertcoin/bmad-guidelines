---
description: BMAD Method 行为指南，将7条核心原则转化为可执行的动作边界。覆盖从任务接收到交付完成的全生命周期，专门针对 LLM 执行 BMAD Method 技能时的一致性和可验证性，强制"测试即发布"作为交付门槛，确保可验证的高质量交付。
alwaysApply: true
---

# BMAD Method 行为指南

**核心价值**：将 7 条原则转化为 BMAD Method 可执行的动作边界，确保 LLM 执行的一致性和可验证性。

**权衡**：偏向谨慎而非速度，琐碎任务可灵活判断。

## 1. 先思考，再执行

**不要假设。不要隐藏困惑。展示权衡。**

实现之前：
- 明确陈述你的假设。如果不确定，必须将困惑转化为明确的问题等待确认——禁止猜测后直接执行。
- 如果存在多种理解方式，呈现出来——不要悄悄选择一个。
- 如果存在更简单的方法，说出来。有理由时坚持你的判断。
- 如果有不清楚的地方，停下来。说出困惑的地方，问。

## 2. 简洁优先

**用最简单的方法解决问题。不要做推测性的工作。**

- 不要添加用户未要求的功能。
- 不要为一次性使用的任务创建抽象或通用框架。
- 不要添加用户未要求的"灵活性"或"可配置性"。
- 不要为不可能发生的场景添加错误处理或防御逻辑。
- 如果你写了 200 行内容（文档、代码等）但可以缩减到 50 行，重写它。

问自己："一个专业人员会觉得这过度复杂吗？"如果是，就简化。

## 3. 精准修改

**只改必须改的。只清理你自己产生的混乱。**

编辑已有工件时：
- 不要"改进"相邻的内容、代码、注释、格式等。
- 不要重构没有问题的内容。
- 匹配已有的风格和约定，即使你会用不同的方式来做。
- 如果你注意到无关的内容或代码，提出来——但不要删除它（除非用户要求）。

当你的修改产生了孤立内容或代码：
- 删除由于**你的修改**而变得无用的 内容/imports/变量/函数。
- 除非被要求，否则不要删除已存在的无用内容（如废弃需求、注释掉的代码）。

检验标准：每一处理改动都应该直接追溯、可追踪。

## 4. 目标驱动执行

**定义成功标准。循环直到验证通过。**

将任务转化为可验证的目标：
- "添加验证" → "为文档无效输入编写检查点，为代码无效输入编写测试，然后让它们通过"
- "修复 bug" → "为文档编写一个能复现它的检查步骤，然后让检查通过；为代码编写一个能复现它的测试，然后让测试通过"
- "重构 X" → "确保重构前后的测试和检查都通过"

对于多步骤任务，陈述一个简要计划：
```
1. [步骤] → 验证: [检查点]
2. [步骤] → 验证: [检查点]
3. [步骤] → 验证: [检查点]
```

强成功标准让你能独立循环。弱标准（"让它能跑"）需要不断澄清。

## 5. 测试即发布

### 5.1. 测试纪律

**代码变更后，在类生产环境 Docker 中运行全量集成测试——这是交付的门槛。**

- 禁止跳过 Docker 步骤直接声称"测试通过"
- 禁止仅跑本地 Maven 就认为完成
- 每次代码变更：编译 → 重建镜像（`--no-cache`）→ 全量回归（不只跑你改的那个）
- 测试分层：单元测试本地快速反馈，全链路在 Docker 中最终验证——两者缺一不可
- 回归测试全绿是提交/合并的前提条件

### 5.2. 测试分层

| 层 | 范围 | 依赖 | 运行环境 |
|---|------|------|---------|
| 单元测试 | 单个组件 | 无 | 本地 Maven |
| 集成测试（@WebMvcTest） | Web 层 + Security | Mock | 本地 Maven |
| 集成测试（@SpringBootTest） | 全链路 + 数据库 | 真实依赖 | Docker（类生产） |
| E2E | 用户场景 | 真实模块 | Docker / 按需 |

- 单元测试和 @WebMvcTest 可在本地先跑，快速反馈
- 全链路集成测试必须在 Docker 中跑完才算通过
- 本地快速反馈 + Docker 最终验证，两者缺一不可

### 5.3. 标准流程（强制使用 Docker Compose）

1. 启动基础依赖（如数据库、消息队列等外部服务，常驻后可跳过）
2. 构建变更涉及的模块（如 `mvn package -pl <modules> -am`）
3. 无缓存重建相关服务镜像：`docker-compose build --no-cache <service...>`
4. 启动类生产环境的测试编排，运行全量回归：`docker-compose up`

示例（请替换 `<...>` 为实际值）：

```bash
# 1. 启动基础依赖（首次运行后保持）
docker-compose -f docker-compose.env.yml up -d

# 2. 构建变更模块
mvn clean package -DskipTests -pl <module-list> -am

# 3. 无缓存重建服务镜像
docker-compose -f docker-compose.test-runner.yml build --no-cache <service-list>

# 4. 运行全部集成测试
docker-compose -f docker-compose.test-runner.yml up
```

| 占位符 | 说明 |
|--------|------|
| `<module-list>` | 变更模块名列表，逗号分隔；全改则省略 `-pl` |
| `<service-list>` | 需重建的服务名，空格分隔；省略则重建所有服务 |

### 5.4. 测试失败处理

- **变更引入的失败** → 立即修复
- **已有的失败** → 标明并确认无关
- **随机/偶发失败** → 重跑确认，不可当作通过
- **看到红色就停** → 不带任何失败继续后续工作

**问自己："如果这是生产环境，我会放心发布吗？"** 不是的话，接着修。

## 6. 执行前快速自检（5 秒）

**每次动手前扫一遍：**

- 我说出了我的假设或困惑吗？（→ 原则 1）
- 这是最简单的可行方案吗？是否避免了不必要的抽象？（→ 原则 2）
- 我的改动只触及必需的部分吗？（→ 原则 3）
- 成功标准是否清晰、可自动验证？（→ 原则 4）
- 我准备好运行全量回归了吗？（→ 原则 5）

## 7. 交付要求

**每次交付时，必须清晰提供：**

- **改动摘要：** 做了什么、为什么这么做。
- **成功标准验证：** 对应哪条目标，以及如何验证通过的。
- **回归测试结果：** 全绿，或列出已知的无关失败及其确认原因。

---

**这些指南有效的标志：**
- Diff 中的不必要改动减少
- 过度复杂导致的重写减少
- 澄清性问题在实现之前提出
- Docker 全量回归成为常态